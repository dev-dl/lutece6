{% extends 'base.html.twig' %}

{% block body %}
    
    
	<h1>Project</h1>
	{{ project.projectName}}
	{{ project.description}}
	<a href =" {{ editProjectURL }} ">{{ editProjectText }}</a>

	<a  href=" {{ addPositionURL }} "> {{ addPositionText }}</a>

	{% for position in positions %}
        <h4> {{ position.title }} </h4>
		<p> {{ position.description }} </p>
		<p> 
            <a href=" {{ path('position',{ slug: position.slug})}} "> View </a>
        </p>

    {% endfor %}  
{% endblock %}